<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EinarTask | Görev Yöneticisi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #fff, #f9f9f9);
            font-family: 'Segoe UI', sans-serif;
        }

        .navbar-brand {
            font-weight: bold;
            color: #ff6347;
        }

        .nav-link.active {
            font-weight: bold;
            color: #ff6347 !important;
        }

        .hero-section {
            padding: 60px 0;
        }

        .hero-title {
            font-size: 3rem;
            font-weight: bold;
        }

        .hero-desc {
            color: #555;
            margin-bottom: 30px;
        }

        .btn-domates {
            background-color: #ff6347;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            font-weight: bold;
        }

            .btn-domates:hover {
                background-color: #e5533d;
            }

        .illustration {
            width: 100%;
            max-width: 600px;
        }
    </style>
</head>
<body>

   
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                <!-- Sol taraf (Metin) -->
                <div class="col-md-6">
                    <h1 class="hero-title">Task Manager Illustration</h1>
                    <p class="hero-desc">Task manager vector design concept. To do list application landing page template.</p>
                    <a href="#" class="btn btn-domates">Get Started</a>
                </div>

                <!-- Sağ taraf (Görsel) -->
                <div class="col-md-6 text-center">
                    <img src="/assets/img/TaskHomeDesign.png" alt="Task Illustration" class="illustration img-fluid">
                </div>
            </div>
        </div>
    </section>

</body>
</html>
